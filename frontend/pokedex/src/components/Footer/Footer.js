import React from "react";
import { Typography } from "@material-ui/core";

const Footer = () => {
  return (
    <div>
      <Typography variant="body2" color="primary">
        Link to data sources: www.Kaggle.com
      </Typography>
    </div>
  );
};

export default Footer;